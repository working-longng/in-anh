@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model List<OrderDetail>
@{
    ViewData["Title"] = "History Page";
    // var count = (int)TempData["Count"];
    var lang = ViewBag.Language == null ? null :  ViewBag.Language as Languages;
 
}
<script src="~/js/history.js"></script>
<link href="~/css/history.css" rel="stylesheet" />
@if(Model.Count == 0)
{
    <div class="d-flex justify-content-center align-items-center">
        Bạn Chưa Có Đơn Hàng
    </div>
}
else
{
    <div class="data-history-list">
        @{Html.RenderPartial("_RenderItem", Model);}
    </div>
}
